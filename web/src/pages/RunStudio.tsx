import { useState } from 'react';import GraphView from '../components/GraphView';import ChatStream from '../components/ChatStream';import { api } from '../api/client';
export default function RunStudio(){const [messages,setMessages]=useState<any[]>([]);const agents=[{name:'A1'},{name:'A2'},{name:'A3'}];const run=async()=>{const teams=await api('/api/teams');if(!teams.length)return;const out=await api('/api/runs',{method:'POST',body:JSON.stringify({team_id:teams[0].id,prompt:'Hello',max_turns:4,max_seconds:30,token_budget:1000,reflection:true})});setMessages(out.messages||[])};return <div><h2>Run Studio</h2><button onClick={run}>Start Run</button><a href='http://localhost:8000/api/snapshot.png?run_id=1'>Download Snapshot</a><GraphView agents={agents}/><ChatStream messages={messages}/></div>}
